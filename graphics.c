#include <stdio.h>
#include "ball.h"
#include "display.h"

// Example usage:
int main() {
    // Render the baseball sprite
    draw_sprite(100, 50, &ball);

    // Display the framebuffer (not implemented here)
    // ...

    return 0;
}